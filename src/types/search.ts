// src/types/search.ts

// Tipo já tratado, pronto para ser usado nas telas (Pessoa 6)
export interface FoodResult {
  id: string;
  name: string;
  brand?: string | null;
  description?: string | null;
}
